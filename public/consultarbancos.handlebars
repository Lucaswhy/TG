    
    <script type="text/javascript">
        var valor = documment.getElementByName("busca").value;
    </script>

	<div id="divprincipal">
        <h1>Lista de bancos: </h1>
        
        <div id="divBusca">
            <input type="text" id="txtBusca" name="busca"/>
            <button id="btnBusca" class="btn btn-primary mt-4" >Buscar</button>
        </div>
        
        <div id="divTabela" class="maxWidth">
            <table id="tableCons">
                <thead>
                    <tr>
                        <br>
                        <hr>
                        {{#each banco}}
                        <div class="container mt-4">	
						    <div class="card">
							    <div class="card-body">
                                <h4>Codigo do Banco:{{codBanco}}</h4>
                                        <p>
                                    Numero da conta: {{numeroConta}}
                                        </p>
                                    <a href ="/deletarbanco/{{_id}}">
                                    <button type="button" class="btn btn-warning mt-4">Deletar Banco</button></a>
                                    <a href ="/editarbancos/{{_id}}">
                                    <button type="button" class="btn btn-primary mt-4">Editar</button></a>
							    </div>
                            </div>
						</div>
						{{else}}

                        {{/each}}
                    </tr>
                </thead>
            </table>
        </div>
        
	</div>